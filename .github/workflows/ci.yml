name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -q

      - name: CLI smoke test (offline, mock model)
        run: |
          vulnhound --version
          vulnhound scan deps examples/ --mock --format sarif --output deps.sarif
          python -c "import json; d=json.load(open('deps.sarif')); assert d['version']=='2.1.0'; print('SARIF OK:', len(d['runs'][0]['results']), 'result(s)')"
